{
  "name": "meni",
  "version": "1.0.0",
  "description": "A discord bot that can help with member engagement",
  "license": "MIT",
  "author": "tommy.nurwantoro@gmail.com",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "build": "tsup src/index.ts --minify",
    "deploy": "npm run build && npx tsup src/commands/*.ts --minify --outDir dist/commands && npx tsup src/deploy-commands.ts --minify && node dist/deploy-commands.js"
  },
  "dependencies": {
    "@aws-sdk/client-ecr": "^3.901.0",
    "@types/node-cron": "^3.0.11",
    "@types/pg": "^8.15.5",
    "@types/redis": "^4.0.10",
    "axios": "^1.12.2",
    "discord.js": "^14.22.0",
    "dotenv": "^17.2.1",
    "node-cron": "^4.2.1",
    "pg": "^8.16.3",
    "redis": "^5.8.2",
    "sequelize": "^6.37.7"
  },
  "devDependencies": {
    "tsup": "^8.5.0",
    "tsx": "^4.20.4",
    "typescript": "^5.9.2"
  }
}
